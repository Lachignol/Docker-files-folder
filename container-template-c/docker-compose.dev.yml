services:
  c-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: c-dev
    volumes:
      # Monte dossier de code local dans /app
      - ./:/app
    tty: true
    stdin_open: true
