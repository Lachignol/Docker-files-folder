# Construire l'image finale sans montage
docker compose up -d --build

# Voir les logs (si ton programme affiche quelque chose)
docker logs -f nom_prog

# Ou lancer en interactif
docker compose run --rm nom_prog

